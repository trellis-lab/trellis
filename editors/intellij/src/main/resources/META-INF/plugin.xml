<idea-plugin>
    <id>com.trellis.plugin</id>
    <name>Trellis Mermaid Preview</name>
    <version>0.1.0</version>
    <vendor url="https://github.com/trellis-mermaid/trellis">Trellis</vendor>

    <description><![CDATA[
        <p>Instant, beautiful diagram previews for your Mermaid files — right inside your JetBrains IDE.</p>

        <h2>What is Trellis?</h2>
        <p>Trellis is a next-generation Mermaid diagram renderer that produces clean, overlap-free diagrams
        with crisp orthogonal edge routing. Where stock renderers let edges tangle and overlap, Trellis routes
        every connection precisely — the result is diagrams that are easier to read and share.</p>

        <h2>Features</h2>

        <h3>Live Preview</h3>
        <p>Open any <code>.mmd</code> or <code>.mermaid</code> file and see your diagram rendered instantly
        in the Trellis Preview tool window. The preview updates on every save — no manual refresh needed.</p>

        <h3>Crisp, Overlap-Free Diagrams</h3>
        <p>Trellis uses precision edge routing to ensure connections never cross nodes or pile on top of each
        other. The result is diagrams that communicate clearly at every scale.</p>

        <h3>Supports Multiple Diagram Types</h3>
        <ul>
            <li><b>Flowcharts</b> — decision trees, process flows</li>
            <li><b>Class Diagrams</b> — UML class hierarchies</li>
            <li><b>Entity-Relationship Diagrams</b> — database schemas with crow's foot notation</li>
            <li><b>C4 Diagrams</b> — software architecture and system context maps</li>
        </ul>

        <h3>Mermaid Compatible</h3>
        <p>Trellis reads standard Mermaid syntax — no migration, no new language to learn.
        Your existing <code>.mmd</code> files work as-is.</p>

        <h2>Getting Started</h2>
        <ol>
            <li>Open any <code>.mmd</code> or <code>.mermaid</code> file.</li>
            <li>The <b>Trellis Preview</b> tool window opens on the right and renders your diagram.</li>
            <li>Save the file — the preview updates automatically.</li>
        </ol>
    ]]></description>

    <!-- Requires only the platform module; works in all JetBrains IDEs -->
    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">

        <!-- Register .mmd and .mermaid as Mermaid Diagram files -->
        <fileType name="Mermaid Diagram"
                  implementationClass="com.trellis.plugin.MmdFileType"
                  fieldName="INSTANCE"
                  language="Mermaid"
                  extensions="mmd;mermaid"/>

        <!-- Preview tool window (right side panel) -->
        <toolWindow
                id="Trellis Preview"
                factoryClass="com.trellis.plugin.PreviewToolWindowFactory"
                anchor="right"
                icon="/icons/toolWindow.svg"
                canCloseContents="false"
                secondary="false"/>

        <!-- Project-level service: owns the JBCefBrowser for that project -->
        <projectService serviceImplementation="com.trellis.plugin.PreviewPanel"/>

        <!-- Application-level settings (auto-preview toggle, theme) -->
        <applicationService serviceImplementation="com.trellis.plugin.TrellisSettings"/>

        <!-- Listen to .mmd file saves at project scope -->
        <postStartupActivity implementation="com.trellis.plugin.MmdFileListener"/>

    </extensions>

    <actions>
        <action
                id="Trellis.Export"
                class="com.trellis.plugin.ExportAction"
                text="Trellis: Export Diagram…"
                description="Export the current diagram as PNG or SVG (Premium)">
            <add-to-group group-id="ToolsMenu" anchor="last"/>
        </action>
    </actions>

    <applicationListeners>
        <!-- Pre-warm WASM resource extraction on IDE startup -->
        <listener
                class="com.trellis.plugin.TrellisPlugin"
                topic="com.intellij.ide.AppLifecycleListener"/>
    </applicationListeners>

</idea-plugin>
